# 本项目运行与数据边界

- 日常运行只使用本机 Windows 正式库和根目录 `config.toml`，不再维护 Mac 演示流程、固定样片或持久测试图库。
- 正式活动路径以数据库 `library_state` 为准；配置里的历史原片根不能直接当作活动目录。禁止修改 `D:\Photo`。
- 自动化回归保留，写入、损坏和恢复实验只能使用自动清理的临时目录/数据库，不得改用正式库。测试不能启动真实模型或干扰 ComfyUI。
- 真实照片的复制、移动、删除、重命名、XMP 或 Lightroom 写入仍需用户明确授权。
- 不操作鼠标，不打开、移动或激活可见窗口；不得遮挡微信小店屏幕。需要服务时使用隐藏后端，不自动打开页面。
- 启动/重启前核对服务及 `/api/tasks/current`，不打断 running 或 paused 任务。升级必须保留并核验自动备份。
- 清理只处理已核实的测试产物，保留正式归档凭据、升级备份、安装包及正式验收记录。自动审批拒绝的永久删除不得绕过，也不得把“移出工程”报告成“已永久删除”。
- 修改后执行 Python 测试、Ruff（src/tests/scripts）、前端测试和生产构建，检查 diff 与暂存内容后提交并推送 main；禁止 force push。
- 完成交接包含结果、验证、未完成事项和下一步；不在普通实施步骤间等待用户重复说“继续”。
