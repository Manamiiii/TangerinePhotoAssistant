# 大图库性能基线

本基线只生成 SQLite 元数据，不生成、读取或修改任何照片，不启动模型，也不使用 GPU。
合成路径以 `SYNTHETIC:` 开头，不能作为真实文件操作输入。默认覆盖 10k、50k 和 100k
Capture，并为每种规模记录 P50/P95、Python 分配峰值、进程工作集峰值和
`EXPLAIN QUERY PLAN`。

## 运行

先选择一个与正式图库、工作目录和缓存目录完全分开的临时目录。Windows 示例：

```powershell
$env:PYTHONPATH = "src"
.\.venv\Scripts\python.exe -m tangerine_photo_assistant.large_library_benchmark `
  --workspace D:\TangerineSyntheticBenchmark `
  --sizes 10000 50000 100000 `
  --iterations 5
```

macOS 示例：

```bash
PYTHONPATH=src .venv/bin/python -m tangerine_photo_assistant.large_library_benchmark \
  --workspace /tmp/tangerine-synthetic-benchmark \
  --sizes 10000 50000 100000 \
  --iterations 5
```

目录中会保存每个规模的可复用 SQLite 数据库和 `large-library-benchmark.json`。再次运行时，
只有数据库 Capture 数与请求规模一致才会复用；工具不会覆盖已有数据库。需要重建时应由操作者
明确删除整个隔离基准目录，程序不提供自动删除参数。工具会写入
`.tangerine-synthetic-benchmark` 标记；若目标目录非空且没有该标记，会直接拒绝运行，避免把
基准文件混入照片、工作数据或其他已有目录。

## 场景

- 图库第一页：验证常用默认排序。
- 图库深页：按有现存 JPG 的可见 Capture 数选择末尾 OFFSET，暴露深页退化。
- 大相册折叠：自动选择可见照片最多的相册，验证代表图、汇总大小与分页，不固定相册 ID。
- 模型问题筛选：选择最新可信结果中实际存在且覆盖最多照片的问题，不测空结果冒充快速查询。
- AI 高风险队列：覆盖 schema 27 的物化风险字段。
- 摄影统计概览：覆盖多项聚合和条件性复盘。
- 相似组待选：覆盖分组聚合、置信度、待选统计与分页。

每个场景在独立子进程内先运行一次，再测多次；记录首次调用耗时，但由于场景选择和操作系统缓存
已经可能读过数据库，不称其为冷启动。没有可用相册或模型问题时对应场景标记 skipped。
v2 将 SQL 跟踪与 Python 内存追踪移出计时区间，分别收集查询计划和内存；工作集峰值在计时后、
诊断追踪前采集。v1 数字包含追踪开销，不能把测量方法变化称为产品提速。
报告保留完整查询计划，仅在本地保存；公开归档使用汇总数字。小样本 P95 接近最大值，须复测观察波动。

## 判定方式

第一轮只建立基线，不凭单次开发机数字直接修改查询。至少比较 Windows 正式运行环境与一台
Mac 隔离环境，并关注随 10k → 50k → 100k 的增长曲线。只有 P95、内存或查询计划表现出稳定
退化时才进入优化；优化前后必须使用相同数据规模、迭代次数、Python 和 SQLite 版本复测。

## 正式库只读测量

正式库完成当前版本备份升级和健康验收后，可把报告写入已经存在的本地运行目录：

```powershell
$env:PYTHONPATH = "src"
.\.venv\Scripts\python.exe -m tangerine_photo_assistant.large_library_benchmark `
  --existing-database D:\PhotoLibrary\AnalysisDatabase\catalog.sqlite3 `
  --output runtime\formal-library-benchmark.json `
  --iterations 5
```

该模式要求数据库 schema 与代码完全一致，只使用只读连接，不运行完整性检查，不读取照片，
也不会创建、升级或修改数据库。报告不包含数据库路径；输出目录必须已经存在，并且拒绝覆盖
同名报告。需要复测时应显式选择新的报告文件名，以保留前后对照。

## 2026-08-24 Windows 开发机基线

环境为 Windows 11、Python 3.12.10、schema 27；每项预热一次后测量 3 次。数字是合成元数据
的工程基线，不替代正式图库实机验收。

| 场景 | 10k P95 | 50k P95 | 100k P95 |
| --- | ---: | ---: | ---: |
| 图库第一页 | 42 ms | 182 ms | 423 ms |
| 图库深页 | 63 ms | 460 ms | 1,005 ms |
| 大相册折叠 | 45 ms | 213 ms | 388 ms |
| 模型问题筛选 | 32 ms | 125 ms | 227 ms |
| AI 高风险队列 | 6 ms | 17 ms | 28 ms |
| 摄影统计概览 | 337 ms | 1,731 ms | 3,471 ms |

统计最初在 100k 下出现约 171 MB 进程峰值。将条件复盘改为流式读取、只关联已有模型结果的
题材后，正式三档复测的所有场景进程峰值均约为 27–35 MB，统计结果未改变。100k 统计耗时仍
超过 3 秒，因此后续应先评估按版本增量汇总；当前图库查询尚不需要引入外部数据库或服务。

## 2026-08-25 正式库升级前只读检查

Windows 正式配置指向的分析库仍为 schema 26，共 13,809 个 Capture。服务关闭时执行完整
`PRAGMA integrity_check` 约耗时 4 分 30 秒，结果为 `ok`；该过程没有升级或写入数据库，也没有
读取照片或启动模型。schema 32 的工作队列和相似组规模化查询依赖后续表结构，因此正式库查询
延迟必须在首次启动完成备份升级并验收健康状态后再测，不能用 schema 26 数字冒充当前版本基线。

## 2026-08-31 正式库验收与查询优化

- 已启动服务为 `ok / local-only / schema 32`，任务空闲；没有重启或触发分析。
- 正式库 13,809 个 Capture，其中 13,806 个有现存 JPG。主库与唯一的
  `catalog-pre-schema32-from26-20260825-141452-294860.sqlite3` 均 `integrity_check=ok`，
  `foreign_key_check` 无错误；schema 分别为 32 / 26，文件大小及修改时间保持不变。
- Python 3.12.10、SQLite 3.49.1；正式库前后均用 v2 方法、7 次采样，计时不启用 SQL/内存追踪。
  最大相册实际是 ID 50（3,696 张，折叠后 2,345 条），模型问题实际命中 59 张。
  修复了旧工具只测 1 号相册 6 条和不存在的“噪点”问题 0 条所造成的误导。

| 正式库场景 | 优化前 P95 | 优化后 P50 | 优化后 P95 |
| --- | ---: | ---: | ---: |
| 图库第一页 | 83.00 ms | 45.41 ms | 47.16 ms |
| 图库深页 | 104.43 ms | 51.24 ms | 52.84 ms |
| 最大相册折叠 | 99.02 ms | 95.72 ms | 98.86 ms |
| 模型问题筛选 | 17.46 ms | 14.81 ms | 15.66 ms |
| AI 高风险队列 | 2.95 ms | 3.01 ms | 3.61 ms |
| 摄影统计 | 607.07 ms | 217.81 ms | 219.47 ms |
| 相似组待选 | 34.76 ms | 34.98 ms | 35.79 ms |

统计 P95 降低约 64%，深页约 49%；未修改的场景有正常波动，不声称每项提速。
统计单场景进程峰值从约 29.3 MiB 增至 38.0 MiB，是语句级关联复用的内存代价；未建立长期缓存。
同一正式库优化前后全部统计字段/顺序一致，图库 4 种排序 × 5 类条件共 20 个查询完整响应一致。
新增测试覆盖空库、缺失 JPG、空 EXIF、评分更新、筛选和跨页，不修改正式人工数据。

10k/100k 合成目录为本机 `runtime/synthetic-performance-20260831`，没有照片或模型任务。
最终 100k 用相同 v2 方法测 5 次：图库首页 P95 330 ms、深页 381 ms、折叠 340 ms、
统计 1,263 ms，统计场景峰值约 59.7 MiB。100k 统计与原实现逐项相等；同环境额外对照
3 次的统计 P50 从 2,450 ms 降至 1,280 ms。不要与 2026-08-24 的 v1 数字直接计算提速比例。

本地原始报告（Git 忽略，含查询计划）：

- `runtime/formal-library-20260831-baseline-v2.json`：优化前。
- `runtime/formal-library-20260831-release-v2.json`：正式库最终复测。
- `runtime/synthetic-100k-20260831-release-v2.json`：最终 100k 复测。

以上为独立只读查询性能，不包含照片解码、HTTP 和前端渲染，也不代表启动速度。
原服务单独 HTTP 实测统计 P95 约 1,014 ms、图库首页约 136 ms；服务未重启，仍加载旧后端，
不能声称优化后的页面耗时已实测。下一次正常重启再验端到端；Mac 性能与 Safari 仍需独立核验。

### 同日 17:18 重启后补测与缩略图反馈

用户手动重启，已只读核验新进程、健康与任务空闲。5 次 HTTP 请求：图库第一页
179.9/50.8/51.9/50.8/51.2ms；统计 231.4/228.7/250.0/228.2/228.3ms。这些数值仍不包含
缩略图解码/传输和浏览器绘制。用户反馈翻页明显改善，但首次图片仍逐张出现、耗时数秒。

缩略图全局串行锁已改为最多 2 张并发解码，并合并同源同尺寸请求；小图/列表改用 320px。
本地隔离对照使用 4000×3000 人工噪声 JPEG、24 个缓存键、6 个调用线程、模拟数据库定位，
3 轮冷缓存中位耗时由 1,355.67ms 降至 686.42ms；热缓存约 10–15ms。测试不使用真实照片，
不能据此承诺正式机械硬盘或浏览器页面提速比例。重启加载新实现后再做真实冷/热页观察。

新统计分档在正式库只读计算约 224ms（单次，不是 P95），只有分档版本、四类参数分布、曝光
补偿分布和条件关联改变；其余结果逐项保持一致。每个分布仍覆盖 13,806 张，27 张缺少等效焦距
归入未知，不混用实际焦距。参数变化不涉及 schema、原片 EXIF 或已有模型结果。

### 同日 19:17 缓存命中抽查

正式后台保持 `7613f6a`、schema 32、任务空闲，未重启；抽查此前已查看的三个 Capture，
顺序请求 320px 和 640px 缩略图，不删除缓存。320px 为约 21KB，请求 119/7/6ms；
640px 为约 74–76KB，请求 7/6/6ms，响应使用 `private, max-age=86400`。
同样顺序复测为 91/6/6ms、6/5/6ms；单次首请求包含客户端调用与连接等开销，未拆分定位。
这只是小样本热路径观察，不是冷页、P95 或整页渲染性能。

前两项候选（响应后维护、大缓存生成小图）已在下一轮实现，见下方隔离对照。用户主动选择
相册的低优先级预生成仍未实现；需暂停与容量上限，不默认扫全库。

### 同日缩略图维护与复用优化（尚未重启正式服务）

可重复命令：`.venv/Scripts/python.exe scripts/benchmark_thumbnails.py --baseline-ref 667ed8f`。
仅在临时目录生成 4000×3000 噪声 JPG 和 4096 个缓存清单项，模拟数据库定位；三轮取中位数。
不会加载正式配置、扫描真实目录或读取真实照片；临时源图字节与修改时间在测试后校验不变。

| 路径 | 优化前 ms | 优化后 ms |
|---|---:|---:|
| 缓存目录统计 | 217.05 | 56.04 |
| 第一个 320px 缓存缺失请求 | 714.29 | 53.20 |
| 已有 640px 再生成 320px | 50.89 | 6.65 |
| 已有同尺寸缓存命中 | 0.76 | 0.82 |

第一张的旧耗时包含同步维护；新版维护延至 HTTP 正文发送后，工作不是凭空消失。
目录统计不执行回收，回收仍有单独安全检查。这个对照不含数据库、HTTP、真实硬盘冷读和
浏览器绘制，不代表所有翻页速度或端到端提速比例；正式后台仍为 `7613f6a`，需安全重启后验收。

### 2026-09-02 下一页有限预加载

正式服务此前已加载 `9cb42d1` 并完成实机浏览；9 月 2 日继续开发时服务已关闭，没有为了前端改动
重新启动。图库当前页可见图片队列空闲 600ms 后，串行预加载下一页前 8 张当前布局缩略图；真正
翻页仍重新请求完整页面数据。浏览器实机开发检查连续点击 1→4 页时上下页码一致为第 4 页，当前
可见卡片均已显示且详情可立即打开；这是功能冒烟，不是冷页延迟对照，不能作为提速比例。

自动测试覆盖前台优先、安静期重置、取消、最多 8 张、320/640px 尺寸、末页边界、503 停止和
禁止原图/外链。该策略用少量空闲 I/O 换取顺序浏览的下一页首屏速度；随机跳页、首次进入、后台
任务非空闲和页面隐藏时不预取，因此仍会存在快慢页差异。
