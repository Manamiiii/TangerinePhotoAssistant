# 当前状态与新 Session 交接

更新日期：2026-09-08。本文只记录当前可执行状态，不保存逐次开发日志；现场仍以代码、Git、
数据库和服务接口为准。

## 代码与运行状态

- 正式分支：`main`；Python 3.12+、FastAPI、SQLite、React/Vite/TypeScript。
- 当前代码 schema：32。
- 2026-09-08 已经用户明确授权导入“她的生日 · 2026”（相册 52，纪念）：从存储卡复制并
  逐文件 SHA-256 校验 956 个文件，新增 478 组 JPG/RAF；正式数据库 Capture 总数为 14,287。
  EXIF 全部完成，扫描错误为 0，数据库完整性与外键正常，原有 ID、评价和标签保持不变。
  用户已在系统触发并完成归档，照片现位于 `D:\PhotoLibrary\Photos\纪念\2026\她的生日 · 2026`。
  956 个目标文件与索引正常，待整理源副本及空批次目录已清理，无复制中间文件残留；照片关联、
  相册归属、评价和标签未改变，数据库完整性与外键检查通过。归档计划及数据库备份保留在
  `D:\PhotoLibrary\AlbumArchive`。导入前备份为 `D:\PhotoLibrary\Backups\catalog-before-birthday-import-20260908-120932.sqlite3`。
- 2026-08-31 已启动的 Windows 正式服务健康接口为 `ok / local-only / schema 32`，任务空闲，
  图库接口显示 13,806 条，数据库有 13,809 个 Capture（其中 13,806 个有现存 JPG）。
  正式库及 `catalog-pre-schema32-from26-20260825-141452-294860.sqlite3` 备份已用只读连接
  核验：版本分别为 32 / 26，`PRAGMA integrity_check=ok`，`foreign_key_check` 均无错误。
  17:18 用户手动重启后已核验新服务健康、任务空闲，上一轮查询优化已加载；未写入人工评价。
  18:48 用户重新打开源码窗口后，正式服务已加载 `7613f6a`，包含缩略图有限并行、统计分档和
  桌面控制协议；健康状态正常、任务空闲。后续本轮只改前端，未再次重启服务。
  此后已补缩略图响应后维护、大缓存降采样复用；用户已安全重启并核验正式服务加载
  `9cb42d1`、schema 32、任务空闲。2026-09-02 再次核对时服务已自然关闭，没有运行或暂停任务。
- 桌面和开始菜单快捷方式已安装，并使用项目内多尺寸品牌图标。
- 已安装可选桌面依赖，源码快捷方式默认打开独立窗口；浏览器入口保留。窗口启动最多等待
  15 分钟，使用 OS 文件锁保护窗口、启动决策和服务生命周期；不会结束占用端口的其他进程。
  关闭窗口不停止后台服务。菜单安全重启核对服务身份、双令牌、在途写入和运行/暂停任务。
- Windows 未签名内测包已构建；隔离空库启动、安全重启、静态资源、设备接口和安装复制通过，
  隔离库 `integrity_check=ok`、Capture 为 0，未扫描照片。用户已确认窗口功能没有发现问题。
  未替换正式快捷方式为打包版；旧正式进程已按用户授权停止，当前正式服务是新版源码进程。
  后续已补构建身份/本地安装包比较、固定日志轮转、校验清单安装、保留配置升级、schema 门禁回退
  和预览式逐版本卸载。卸载仅删除未改动的已登记程序文件，不递归删目录；收据和用户文件保留。
  公开发布前仍需签名、许可汇总、系统卸载入口及干净机器验收，见 `WINDOWS_APP.md`。
- 当前自动门禁：Python 157 项（本机 1 项真实符号链接测试因权限跳过）、前端 95 项、Ruff、
  前端生产构建；2026-09-08 本地通过。链接边界另有不依赖建链权限的测试。
  GitHub Actions 覆盖 Linux 与 Windows，Mac 隔离样例为手动冒烟。
- 暂停任务继续占用当前任务槽；新扫描、视觉、技术及模型任务不能覆盖它，模型/迁移恢复
  必须匹配当前暂停任务的类别与运行 ID。后台任务及模型监控的连接初始化异常会报告失败，
  不遗留运行中状态。相似组评价后的刷新共用当前完整筛选和请求代次保护，不再单独使用
  保存时的旧筛选请求覆盖列表。新增回归只使用隔离目录、模拟失败和前端请求；未启动正式服务。
- 聚合数据按当前页面独立加载，隐藏页面不请求；评分、标签、分组、相册及复核状态按影响范围
  使数据失效，返回页面时重新读取。首页最近 8 张照片和相似组摘要使用独立的全库查询，
  不沿用上次图库分页/筛选。相册跨页面数量按需读取，未返回时显示占位而不当作 0 禁用导航。
  读取失败可显式重试，过期响应被丢弃。普通无相册筛选的照片页初始主 JSON 请求由 16 类降为
  4 类（含任务状态，不含缩略图），这是代码请求范围核对，不是正式库端到端延迟实测。
- 任务状态采用串行轮询：请求结束后，运行中等待 1.2 秒、暂停等待 3 秒、其他状态等待 5 秒
  再读取；隐藏页面取消读取，恢复可见、窗口获得焦点或网络恢复时重新核对。读取超时为 10 秒，
  失败按 5–30 秒退避并支持手动重试。任务控制响应使旧读请求失效，完成状态重复返回不会重复
  刷新数据或重新显示已收起的完成卡；状态读取失败时暂停图库预加载。慢请求、取消和状态转换
  由模拟测试覆盖，尚未进行正式服务双窗口实测，未为验收启动分析任务。
- 已同步 Cloud PR #3：图库点击进入沉浸看图，支持缩放拖动、相邻预加载、跨页导航和关闭后定位。
  已修复跨页过期请求、组内越界、详情缓存过时、图片加载状态、缩放归位与列表组入口遮挡。
  正式服务只读验证了跨页往返、缓存图显示、拖动归位、电脑全屏进出和关闭定位；慢请求与
  组内边界由自动测试覆盖，Mac Safari 和真实写后往返交互不在本轮验收范围内。
  分支摘要、提交号、修复建议与专项验收见 [Cloud 分支归档](archive/2026-08-31-cloud-branches.md)。
- 相册编辑保存成功后才关闭；标签、修图方案及模型复核备注保存失败时保留输入并提供局部错误，
  保存过程防止重复提交。相册、设置和详情草稿接入离开保护，覆盖侧栏切页、历史导航、切图、
  关闭和浏览器离开提示；在途保存阻止应用内离开。无关详情刷新不再重置标签或修图草稿。
  通用弹窗与照片详情共用可见控件焦点循环和关闭后焦点恢复，照片快捷键忽略组合键及输入法组合。
  新增 jsdom 交互回归；实际 Windows 窗口缩放、原生确认框及浏览器历史交互仍需界面验收。
- 修图预览按图片 URL 隔离加载状态，失败或等待 15 秒后提供手动重试，不清空参数；按住原图
  对比支持空格/回车，松开或失焦恢复参数预览。首页最近照片、摄影摘要及统计页区分加载、失败
  与空数据。相似分组更新在任务暂停或状态未知时禁用并说明原因。以上使用模拟图片事件与 DOM
  回归验证，未启动正式服务或分析任务。
- 图库批量管理增加“查看已选”：按勾选顺序每页核对 40 张，包含跨页及不同筛选条件下的选择，
  展示缩略图、相册与日期，可逐项移除勾选。清单重新只读查询当前页的显式 ID，索引缺失及无
  现存 JPG 的记录仍保留提示；关闭/换页取消读取，失败可重试，不改变选择。继续限制最多 500
  张，不扩展为“全部匹配”，移除勾选不修改照片。已通过隔离数据库及 DOM 回归，待真实窗口验收。
- 已选清单可跨页加入两张对比候选，并排查看、同步缩放/拖动及方向键移动；适应窗口会归中，
  单张失败可独立重试。返回清单保持勾选并恢复键盘焦点；对比使用接近窗口大小的弹窗，支持
  左右并排、上下排列和单图 A/B 切换，切换布局归中，切换 A/B 保持缩放。使用现有最长边
  1280px JPG 预览，倍率相对适应尺寸，不是原片像素级查看，也不自动对齐主体。未改变照片评价，
  未加载 RAW 或运行分析；DOM 回归通过，真实图像与 Windows 缩放视觉验收尚未进行。

- 演示启动脚本额外生成 100 张分页样片，共 130 张 JPG；默认核心 fixture 仍保持原有数量，
  分页样片按需追加且重复运行不覆盖既有登记样片。本机另备独立验收库
  `runtime/ui-acceptance-20260907/config.toml`，已核验 130 个 Capture，40 张一页的最后一页有 10 张。
  未改动正式库或旧演示库；本轮真实截图验收因浏览器自动化无可用实例而未完成。

- 图库分页仅保留底部一处。对比布局和 A/B 切换复用统计、设备等页面的 `section-tabs` 分段样式，
  保留选中态、宽松间距及窄屏换行；相册组内的“双图对比”和“调整分组”合为右侧操作区。相似组页面和图库组内
  弹窗新增“双图对比”，可选择组内不同 A/B 照片，复用三种大窗布局，返回不改变选片结论。
  用户已确认此前其他验收项目正常；本轮新增入口通过 DOM 回归，未操作可见窗口。

## 已实现的产品闭环

- 相册内新增“完成归档”：预览类型/年份/相册名目录及全部文件，显式确认后备份、复制校验、
  更新稳定索引并清理对应待整理源文件。异常可继续，未完成归档阻止其他写入和扫描。
  当前支持全部照片仍在待整理的相册；不覆盖同名冲突，不处理混合正式/待整理来源。
  已用隔离 JPG/模拟 RAW 验证完整流程、再次扫描、路径边界、变更/损坏、复制及提交中断、
  清理占用恢复和 API 任务互斥。正式相册归档由用户在页面触发，助手只读核验完成结果；
  本轮修复未操作鼠标或可见窗口。
  相册标题下提供突出归档提示区；相册列表按实际索引和未完成记录显示待归档、部分待归档、
  归档未完成或已在正式目录，并提供“去归档”入口。后台分别报告复制校验、提交前复核、
  更新图库路径及清理待整理源文件的进度，避免复制完成后长时间停留在满进度。

- 本地只读索引、增量扫描、批量 EXIF、JPG/RAW Capture 配对和相册管理。
- 元数据连拍候选、JPEG 视觉指纹、相似组拆分、推荐排序、人工选片、历史恢复、低风险批量
  预览/撤销和稳定抽检。
- 技术检测、本地模型批次、模型风险审计、固定基准集、人工可信度复核和版本扩大门禁。
- 题材、工作状态、人工问题、地点、保留理由和相册附件；人工、分析和导入来源隔离。
- 照片详情分层信息、扩展 EXIF、JPG 亮度直方图、沉浸查看、参数解释和参数化修图预览历史。
- 摄影统计、条件性问题关联、成长趋势、选片会话与修图采用反馈。
- 图库常用视图、分页、折叠相似组、逐页最多 500 张的批量管理，以及 JPG/RAW 隔离导出。
- Lightroom 目录只读预检和 CSV/JSON 准备清单；不打开目录数据库、不生成 XMP。
- 历史/活动图库完整性基线、差异调查、后台任务异常队列、脱敏诊断和便携人工数据备份恢复。
- 设备库存 CRUD、图库 EXIF 使用量、相册附件关联和开放授权的内置器材图片。
- Windows 独立窗口、安全启动管理、未签名内测包、首次设置向导、Mac 隔离样例和
  10k/50k/100k 合成性能基线。

## 大图库设计结论

万张级使用不依赖人工逐条查看全量结果。默认流程是“聚合 → 风险排序/稳定抽样 → 人工队列 →
相册或单张下钻”：

- 首页按每日预算展示建议量和总积压，不把全部历史问题包装成当天任务。
- 技术、模型、完整性和任务异常均有稳定状态、重新出现语义与可找回历史。
- 图库和相似组在 SQLite 中完成筛选、折叠和分页；前端不加载全部 ID 执行隐式批量写入。
- 文本搜索防抖，分页请求可取消并带请求代次保护；写后只失效相关领域。
- 已完成 schema 32 正式库只读基线及 10k/100k 合成复测；统计复用单次查询内的照片关联，
  图库先分页 ID 再计算展示字段，未引入长期结果缓存、数据库迁移或新分页语义。
  数值、等价性与测量方法见 [性能基线](PERFORMANCE_BASELINE.md)。
- 缩略图生成最多同时解码 2 张，同图同尺寸请求合并；命中缓存不解码，列表/小图请求 320px，
  中图/大图保留 640px。没有全库预生成、后台预取或 GPU 任务。
  新版优先从同源版本的大尺寸缓存生成小图，不放大、不改变方向；损坏衍生图回退只读原片。
  缓存维护改为图片发送完成后执行，同一时刻最多一次、冷却 30 秒，遍历复用目录项元数据。
  近期 30 秒访问的缩略图暂不回收，容量目标可短时超出；后续图片请求重试回收。持续清理失败
  会限制新缓存增长并返回可重试 503，不影响已有缓存命中；不引入永久后台线程或全库预生成。
- 图库页码使用请求目标而不是上一份响应，加载时仍能连续翻页；页码输入回车/失焦提交，
  移除搜索后的重复分页防抖。上下分页条、骨架占位和页内失败重试避免误操作上一页照片。
  图库可视区域及临近缩略图最多 4 个下载，切页取消排队及在途下载，释放对象 URL；
  浏览器缓存仍复用。实机浏览器已核验连续 1→4 页、输入第 12 页和版本显示，未写人工数据。
  当前页前台图片全部空闲 600ms 后，可串行预加载下一页前 8 张缩略图；预加载只有一个低优先级
  槽位，新可见图片直接抢先。切页、改筛选、改显示尺寸、隐藏页面、打开详情、开始后台任务或
  30 秒超时都会取消剩余项；失败不重试、不显示错误，也不持有预取的评分或分组数据。
  设置顶部也显示后台版本、构建、schema，并说明窗口“应用”菜单和离线包检查入口。
  版本正文与标题共用 24px 水平内边距；系统维护页统一 20px 模块间距，避免标题卡与异常卡贴合。
- 参数分布与条件关联共用 `parameter_buckets.py`，按常用参数边界分档，焦段统一使用 EXIF
  的 35mm 等效值，缺失不混入实际焦距。原始 EXIF 保持不变，无需重扫或重跑模型。
  新条件键带 v2 版本；旧常用视图/深链接保持旧区间语义。观察卡片使用独立标题和等宽网格。

## 当前代码结构评价

数据安全边界、稳定 `capture_id`、领域服务和查询模块是可靠主体。维护热点按优先级为：

1. `webapp.py` 仍约 140 KB，应继续拆 router、请求模型和任务编排，领域写入不再回流路由。
2. `web/src/main.tsx` 仍约 63 KB，应把剩余加载和写操作迁入 feature controller/hook。
3. `web/src/styles.css` 仍约 184 KB，应按 feature 拆分并建立响应式/视觉回归后再批量整理。
4. TypeScript 契约仍手工维护；公共发布前应由 OpenAPI 生成，减少前后端漂移。

2026-08-25 已清理无调用方的旧 events/bursts/duplicates/phone-share API 别名、一次性 AI 截止
守护脚本和被现行路线图取代的历史计划文档。数据库表和迁移恢复能力没有删除。

## 正式数据安全边界

- 不修改 `D:\Photo` 中任何文件。
- 未明确授权不复制、移动、删除、重命名或覆盖真实照片。
- 不自动写 XMP，不自动操作 Lightroom 目录。
- 不干扰 ComfyUI 或其他 GPU 任务，不自动启动全量技术或模型分析。
- 设置可随时修改路径，但只在重启后生效；不自动搬运旧工作区、数据库或缓存。
- schema 升级必须在任务空闲时通过正常服务启动，并核对升级前备份、完整性与健康接口。

## 下一次 Windows 验收

1. 启动前读取 `/api/tasks/current`；若存在 running/paused 任务，不重启或中断。
2. 服务未运行时才通过桌面快捷方式启动；当前服务已报告 schema 32，不重复执行升级。
3. 2026-08-31 主备库完整性及健康检查已通过；正常重启加载查询优化后，只需先核对健康/任务
   状态和图库、统计页面，不为同一版本重复升级或每次全量完整性检查。
4. 完成 [WINDOWS_ACCEPTANCE.md](WINDOWS_ACCEPTANCE.md) 的当前回归项；不运行全量模型。
5. 查询优化重启后的 HTTP 已补测：图库 5 次中位约 51ms、统计约 229ms，不含图片加载。
   当前缩略图新实现已加载并验证真实页面；首次生成仍有等待。已缓存的三张 JPG 小样本 HTTP
   抽查多数约 6–7ms，不是全图库 P95 或冷缓存指标；后续重点应区分生成等待与缓存命中耗时。

## 新会话启动检查

```powershell
cd D:\IdeaProjects\TangerinePhotoAssistant
git status --short --branch
git log -5 --oneline --decorate
git fetch origin
try { Invoke-RestMethod http://127.0.0.1:8765/api/health } catch { "service not running" }
try { Invoke-RestMethod http://127.0.0.1:8765/api/tasks/current } catch { "task endpoint unavailable" }
```

修改后运行 Python 测试、Ruff、前端测试和生产构建；提交前检查 diff 与暂存文件，不 force push。
