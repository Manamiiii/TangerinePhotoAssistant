# 产品路线图

本文只记录尚未完成的产品目标与实施顺序；已经交付的实际状态以
`CURRENT_STATE.md`、代码、数据库和接口为准。`PRODUCT_PLAN.md` 保留完整产品设想，
不再把其中早期的“当前进度”段落当作现场状态。

## 稳定边界

- 原片默认只读，人工结论优先于模型结论。
- JPG、RAW 和附属文件共享稳定的 `Capture` 身份。
- 不自动删除、移动、写 XMP 或操作 Lightroom 目录。
- 人脸身份聚类暂缓；选片所需的眼睛、姿态证据不得建立人物身份档案。
- 新能力优先使用隔离样片和 Mock 数据验证，再安排正式数据回归。

## 实施顺序

### 1. 数据与工作流基础

- 多维标签：题材、工作状态、人工问题、地点；人工、分析和导入来源分开保存。
- 统一星级语义、入选/排除与后期状态，避免同一概念出现多套相互矛盾的字段。
- 相册或照片组级器材使用上下文，支持脚架、滤镜和闪光灯等 EXIF 无法确认的附件。
- 写入或导出操作统一进入可预览、可确认、可审计的操作清单。

当前进度：schema 21 已建立 Capture 多维标签、预置词、自定义词、状态单选和来源隔离；
照片详情可以人工维护标签，Mac 隔离图库包含示例数据。图库可按四个维度组合筛选，临时
多选支持批量添加或移除人工标签，批量设置状态会替换旧状态。schema 22 已支持为相册人工
记录脚架、滤镜和闪光灯等拍摄附件，并在设备管理反向统计使用相册数；机身和镜头仍以 EXIF
为准，不把技术检测结果自动写成人工标签。schema 23 已统一星级、入选/排除与工作状态语义，
兼容保留历史冲突状态但不再允许新增。连拍对比已可在稳定推荐顺序与拍摄顺序间切换，并用
已有分项技术分展示相对推荐理由；同时区分技术最佳、接近备选、普通候选和明显技术弱项，
不会改写人工入选或自动排除。摄影统计已按完成人工入选的相似组计算 Top‑1 命中率与 Top‑2
覆盖率，未选组不计为失败。schema 24 已增加动作、表情、构图、关键瞬间和叙事补充等人工
保留理由，并在统计中汇总分布。组内新增可选“兼顾差异”顺序：只在技术合格的接近候选中
结合已有 JPEG 指纹提示差异复核，不把视觉距离冒充为动作、表情或构图识别，也不自动入选。
后续继续用真实选片理由评估该辅助顺序是否有效。

### 2. 选片智能

- 在现有相似组和技术推荐上增加稳定组内排序。
- 区分最佳、备选和明显失败，展示相对原因。
- 保留动作、表情与构图多样性，避免只留下数值接近的重复画面。
- 建立人工基准，评估分组误判、漏检和首选命中率。

### 3. 题材与拍摄复盘

- 本地题材多标签，不要求每张照片只有一个类别。
- 把“现在如何修”与“为什么发生、下次如何拍”拆成独立结构。
- 复盘保存现象、原因、画面/EXIF 证据、置信度、可修复程度和下次建议。
- 汇总某焦段慢快门、逆光欠曝、多人景深不足等条件性高频问题。

当前进度：详情查询已兼容现有 v4 模型结果，分开展示画面观察、下次拍摄和后期建议，并保留
模型/技术/规则来源、置信度与可修复程度；旧结果不需要重跑。摄影统计已汇总每张照片最近一次
可信完成结果并支持按具体模型观察钻取。已有结果可同步为独立分析题材，v5 支持 1–3 个题材；
条件性统计已覆盖题材、焦段、快门、ISO、光圈和器材，带最小样本门槛与精确钻取。参数化
修图 MVP 与差异辅助排序均已进入可回归状态；更强的语义多样性仍等待真实人工选片基准积累。

### 4. 参数化修图

- 曝光、白平衡、高光、阴影、降噪、锐化、裁剪和水平校正参数。
- 生成低分辨率非破坏性预览，不写原片或 XMP。
- 保存建议值、人工最终采用值和未采用原因，形成个人偏好反馈。
- 复杂蒙版、移除、液化和皮肤处理仅生成 Photoshop 待办。

当前进度：schema 25 已提供参数化方案历史；当前 `tangerine-preview-v2` 详情页可从 v5 模型建议
载入曝光、对比度、高光、阴影、色温、色调、饱和度和锐化起点，人工调整后保存草稿、标记
采用或暂不采用并记录原因。每次保存形成不可覆盖的历史版本，并可把旧版本恢复为新草稿。界面快速预览
并增加近似降噪、居中裁剪和水平校正，共 11 项。旧 v1 历史自动补零兼容。预览只基于缓存
缩略图，明确不冒充 Lightroom 算法，也不写照片或 XMP。下一步只需真实视觉验收校准映射。

### 5. 成长分析

- 同题材、相似条件下的人工高星率、技术失败率、重复拍摄率和选片效率。
- 参数、器材、附件与失败问题的关联，以及采纳建议后的改善幅度。
- 人工评分作为主指标；模型版本变化不得制造历史趋势。

当前进度：第一期已建立整体与按拍摄月的稳定基线，包含人工 4–5 星率、已有质量结果中的
技术低分率、进入当前相似组的照片占比，以及明确入选/排除后的人工保留率；每项同时展示
分子、分母，缺少样本时显示为空。界面不自动把月份差异解释为进步或退步，并明确保留率不
等于选片耗时效率。现在已支持按至少 3 张样本的题材对照，并从 schema 26 开始记录选片会话
的活跃耗时和决策次数；至少完成 3 个会话才显示平均值，历史耗时不反推。修图采用反馈也按
每张照片最新方案汇总，但改善幅度仍需积累跨月份、同题材和相似条件的后续人工结果后再比较。

### 6. Lightroom 安全衔接

- 先统一 manifest、兼容性校验、字段级合并、备份和恢复。
- 再开放人工确认后的 XMP 生成；不直接写 Lightroom 目录。

当前进度：应用设置已可修改 Lightroom 目录文件夹与目录备份文件夹。后期输出页会只读检查
顶层 `.lrcat`、配套 `.lrcat-data` 和 `.lrcat.lock`，但不会打开目录数据库或删除锁。CSV/JSON
清单已按字段标注评级、旗标、关键词合并、RAW XMP sidecar 候选、已有 XMP 和冲突复核数量；
JPG 保持目录内处理，因为 Adobe 默认会把这类格式的 XMP 写入文件。所有执行字段仍固定为 0。
下一步必须先确认目标 Lightroom Classic 版本和实际目录，再设计隔离 XMP 合并、备份与恢复；
未完成这些验收前不开放写入。

### 7. 公共发布

- 首次启动向导与原生目录选择已完成；Windows/macOS 安装与签名仍待实现。
- 跨品牌标准 EXIF 合成样例、schema 1–25 迁移矩阵和 Capture 级人工数据恢复已完成；授权真实 RAW/HEIC 与相册冲突恢复仍待补充。
- 脱敏诊断包与持久运行日志隐私审计已完成；许可证、贡献规则与封闭测试仍待进行。

## 后置能力

- 人脸身份聚类和人物命名。
- PSD、视频和复杂衍生文件的完整工作流。
- 自动应用修图、自动 XMP、自动移动或删除原片。

## 工程与大图库治理待办

以下项目来自 2026 年 8 月的工程审查。已完成的数据库侧折叠分页、AI 审计字段化、
风险/抽样审计入口、完整性差异明细、浏览器写请求保护和 SQLite 锁等待策略不再列入。
其余工作按小批次实施，保持现有 URL、数据与安全边界：

### 实施门禁

后续项目不再只记录“做什么”，每个批次开始前必须同时写明触发条件、验收指标、人工预算和
停止条件。默认边界如下：

- 性能改动先在脱敏 10k/50k/100k 数据集记录 P50/P95、SQLite 查询计划、进程峰值内存和
  返回条数；不以单次开发机耗时作为通过依据，也不为尚未出现的瓶颈预先引入外部服务。
- 人工队列默认只展示“新发现、重新出现和已到期”，允许确认、忽略、稍后处理和解决；同一
  问题使用稳定指纹去重，必须保存首次/最近发现时间，不能在每次检查后制造一份新待办。
- 普通模型版本先固定复核 30–50 张分层样本，高风险结果全部进入队列；结构错误超过 1%、
  人工判定不准确超过 10% 或出现任何越权操作建议时停止扩大批次，阈值以后按真实基线调整。
- 相似组低风险批量确认默认关闭。至少积累 100 组人工结果并达到预先登记的 Top-1/Top-2
  门槛后才开放预览；每批必须可整体撤销并随机复核，任何操作都不得自动删除或排除照片。
- 首页以每天约 30 分钟可处理量估算成本，不把全部历史积压渲染成当日必须完成的任务；超过
  预算的工作进入稍后处理，不通过增加更多卡片掩盖积压。
- 每个批次只改一个可回滚边界，必须通过 Python 测试、前端生产构建、隔离数据库迁移和
  Windows/Mac 对应冒烟项，之后才进入 `main`。

### UI 与交互后续优化

2026 年 8 月已完成第一轮全局可读性、键盘焦点、低动态偏好、移动导航标签、主页面 Hash
历史、通用弹窗焦点管理、设置未保存提示，以及相似组键盘打开能力。以下较大项目继续分批实施：

- 已将 Hash 页面历史扩展为可恢复相册、筛选、分页和照片详情的完整深链接；未知页面、非法枚举、
  日期、页大小、偏移量、相册或照片 ID 会回退到安全默认值。
- 为所有异步页面建立统一骨架屏、局部刷新和错误重试，避免全量快照刷新及空状态闪烁。
- 将移动端主导航改为经过真机验证的固定底栏或可折叠菜单，处理安全区、横屏和超窄屏。
- 把图库筛选拆成常用与高级两层，增加输入防抖、筛选预设和批量操作吸附栏。
- 为照片详情增加固定分区导航，进一步压缩移动端首屏，并为详情弹层补齐完整焦点循环。
- 统一危险操作确认、撤销反馈、按钮状态和任务完成通知；为隐藏、删除及完整性操作明确影响范围。
- 增加前端无障碍与响应式自动化测试，覆盖 Tab 语义、键盘选片、弹窗、主题和 320px–1440px 布局。

### P0：建立真实性能基线

当前进度：已提供不生成或读取照片的 schema 27 合成基线工具，并完成 Windows 开发机
10k/50k/100k 三档首轮测量与查询计划采集。条件复盘统计已改为流式读取，100k 场景的进程
峰值由约 171 MB 降至约 35 MB；Mac 对照和正式库只读实测仍待完成，详见
`PERFORMANCE_BASELINE.md`。

- 使用脱敏生成数据建立 10k、50k、100k Capture 查询与峰值内存基准，覆盖折叠图库、
  模型问题 JSON 筛选、统计聚合和深页浏览，并保存 `EXPLAIN QUERY PLAN` 回归结果。
- 将 AI 审计聚合从请求时 SQL 汇总继续演进为按版本增量汇总；先用基准确认是否必要。
- 评估图库深页改用游标分页，明确排序稳定性及新增数据造成的重复、遗漏行为。
- 把前端全量快照刷新拆成图库、相册、分析、设备、设置和维护等领域级失效；任务结束只刷新
  受任务类型影响的领域。

### P1：统一任务和人工治理

- 建立持久化通用任务模型，统一扫描、视觉、质量、AI、迁移和完整性检查的恢复、暂停、
  取消、失败与保留策略；任务状态稳定后再以 SSE 取代轮询。
- 已完成第一批人工工作队列：技术检测项与模型风险支持“新发现、待处理、已确认、已忽略、
  稍后处理、已解决、重新出现”，状态只在人工操作时落库；首页使用每日预算，图库可保存常用
  查询视图。质量读取错误已接入相同队列，原始错误不进入 API 或便携备份；技术和模型队列已按
  首次发现时间优先并显示积压年龄。完整性异常已在 schema 29 接入独立调查状态，只有消失后
  再现或差异类型改变才重新进入队列。后台任务失败已在 schema 30 按任务家族与脱敏错误类型
  收敛；人工处理后复发会重新进入队列，同类任务成功会自动解决。
- 为模型审计增加按模型版本、题材、相册、月份、置信度和问题类型的分层抽样、固定基准集、
  覆盖率、版本人工结论及扩大批次的质量门禁。
- 为相似组增加置信度与积压年龄分层、低风险批量接受预览、撤销清单和随机抽检；保留
  “全部人工确认、仅高风险、按相册确认”等策略。
- 为完整性事件增加目录聚合、完整 CSV/JSON 导出、跨检查比较、调查状态和恢复后复检。
- 首页改为展示新增积压、趋势、最高优先级异常和预计处理成本，避免把全部历史未完成量
  当作即时待办。
- 定义人工审核预算、模型批次上限、错误率停止条件，以及数据库、缓存和备份增长背压。
- 跨页批量操作必须以服务端查询快照、影响范围预览、持久任务和撤销清单实现；这些条件完成前
  继续只允许显式选择页面中的照片，禁止前端偷偷加载全部匹配 ID。

### P1：降低工程复杂度

- 按 library、analysis、albums、system、exports 逐步拆分 `webapp.py` 的 router、schema、
  service 和 task runtime，不进行一次性重写。
- 将 `main.tsx` 页面状态、加载、分页和写操作迁入 feature hooks/controller，根组件只保留
  导航、主题、全局任务和详情弹层。
- 将数据库升级改为启动期执行的显式逐版本迁移；普通写连接只配置连接策略。
- 从 FastAPI OpenAPI 生成 TypeScript API 类型，减少前后端手工同步。

### P1：质量与发布

当前进度：Linux/Windows 的 Ruff、Python 测试、前端测试和生产构建已建立自动 CI；Mac
隔离样例提供手动启动冒烟并核对健康/schema/任务接口。正式 Windows 启动、安装器与签名仍待
实机和发布阶段处理。

- 增加前端测试和 lint 基线，优先覆盖图库筛选/折叠分页、乐观评分回滚、分组撤销、
  首次配置及长任务状态。
- 在持续集成中执行 Python 单测、Ruff、前端构建与前端测试，并补 Windows 启动和
  macOS 隔离样例冒烟测试。
- 定义模型结果、日志、报告、任务记录和数据库备份的保留、归档与清理预览策略，并定期
  演练人工数据恢复。
- 公共发布前完成安装器、签名、许可证、贡献规则、隐私说明及授权 RAW/HEIC 样本验证。
