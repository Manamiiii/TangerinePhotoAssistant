# 2026-09-22 质量审查修复记录

基于 `431fd65` 的审查，按批处理已确认缺陷与清理。第一批为 H01–H05，后续记录见下文。

| 问题 | 处理结果 | 隔离验证 |
|---|---|---|
| H01 读取失败被当作缺失 | 访问错误使整批索引回滚，扫描标记失败并保留错误记录 | 注入遍历失败，原文件索引逐列不变，新发现文件也不提前提交 |
| H02 缺失照片身份及人工数据丢失 | 保留缺失 Capture 与文件关联，不按派生结果白名单删除；多个已有 Capture 合并时阻止执行 | 暂时消失/重新出现、只有人工标签、评分保留、冲突事务回滚 |
| H03 补 RAW 改变旧相册 | 导入分配同时要求文件首次发现于本批且 Capture 在扫描前不存在 | 旧 JPG 补 RAW 保留旧相册，真正新照片进入目标相册 |
| H04 校验前写数据库 | 配置与实际 active_root 均在创建/升级数据库前验证 | 非法工作区无新增目录；非法活动根不升级旧 schema、不创建升级备份 |
| H05 归档后旧备份无法匹配 | schema 33 记录历史路径别名，v1 人工备份可匹配旧键；歧义阻止写入 | 归档前备份归档后恢复；重复路径歧义、事务回滚、升级备份与外键 |

新增 11 条数据生命周期回归。先运行最初 6 条确认旧代码全部失败，再验证修复；其余覆盖升级、
歧义及失败边界。测试均在临时目录和隔离数据库执行，没有操作真实照片或正式数据库。

## 运行与兼容边界

- 正式服务未运行，本批没有启动或重启。最终代码 schema 34；正式库最后记录为 schema 32，尚未实机迁移。
- 下一次正常启动会先生成 schema 32 备份，再填充别名；升级不扫描照片、不执行模型或归档。
- 别名从升级时已知路径开始积累，不推测升级前已经丢失的路径历史。不自动修复已脱离的评分，
  不根据相同文件名跨库匹配。跨库身份/备份格式扩展仍属于单独设计。
- 任一目录访问失败时本次扫描整体回滚，这是保守选择；不提供部分更新冒充完整成功。
- 中断后的导入批次接续（M01）、部分跨相册分组（M02）、跨文件恢复（M05）等不等于本批已完成，
  处理结果见后续批次。保留策略、归档安全撤销及大范围结构重构不在本批实施。

## 审查更正

原报告 L08 根据 pyproject 中未显式配置 select 推断 Ruff 规则未启用，这一推断不成立。
本机 Ruff 0.16.4 的 `--show-settings` 已确认 SIM117、TRY 等规则启用。撤回“忽略项无效”的结论，
不据此删除现有豁免；保留“明确依赖/规则版本、CI 可扩大覆盖 scripts”的工程建议。

## 第二批：请求状态、分页与构建清理

- M10–M14：超过 100 张时提前提示 ZIP 导出上限；任务异常、完整性差异和相似组详情忽略失效请求；模型结果与归档状态显示失败和重试；评分同分使用 ID 稳定排序。
- 目录位置不再冒充文件完整性证明：“待整理目录外”与完整性检查明确分开。
- L01–L03、L06–L07：删除重复 Vite 产物、无引用样式和 MetadataReader；筛选按钮使用 group/aria-pressed，避免不完整 tab 语义；构建工具移入开发依赖。
- M17 部分：前端直接依赖固定到锁文件中的现有版本，明确 Node 版本要求；Mac 启动在依赖清单变化后重新安装。Python 跨平台完整锁定尚未完成。
- 新增 5 条前端异步/失败回归、1 条评分分页回归。Mac 启动脚本仅静态核对，未在 Windows 启动服务或冒充 Mac 实机验收。

## 第三批：失败恢复与外部进程边界

- M02：部分跨相册移动连拍候选会在写入前拒绝；整组移动保留相似组与人工选片。同相册操作不受影响，不自动拆散既有组或改写历史。
- M03：损坏或未知格式的归档记录使系统进入写入暂停状态，指出具体记录；图库和相册可继续读取，相册状态标为待核对。保留所有记录和照片，不自动删除坏 JSON；恢复有效记录后再重启。
- M05：恢复前同时留存设备清单；提交或清单写入失败时回滚数据库并补偿清单。覆盖已有/原本不存在清单两种情况。突然断电的跨文件原子提交仍需要持久恢复协议，未声称已解决。
- M07–M08：ExifTool 批次与缩略图提取设 60 秒超时；只终止本次启动的 ExifTool；正常完成取消计时器并回收管道。ComfyUI 检测不再限定 Documents 路径；检测不可用明确阻止模型启动。
- M15 部分：已有当前 schema 的普通连接不重复执行 DDL/预置数据写入；保留旧版升级与备份路径，未把全部历史迁移一次重写。
- 新增提交失败、超时、检测失败、损坏记录和连接 SQL 追踪回归，全部使用隔离数据或测试子进程。

## 第四批：导入接续、任务控制与门禁同步

- M01：schema 34 的 `import_batch` 持久保存目标相册、扫描起点和原有 Capture 集合。元数据/配对/分配中断可从原相册重试，重启会显示提示；取消同样保留本批意图，下一次仍从原相册接续，不会自动扫描。先分配新照片再重建连拍，意图清除与相册分配同事务提交。
- M09 部分：暂停/取消已通过鉴权和归档保护后直接向现有任务发送控制，不等待慢 ZIP 导出；启动、普通写入和安全关闭仍共享写锁。并发测试模拟慢导出验证控制及时响应、普通写入仍排队、缺失令牌仍拒绝。
- M17：新增已测试的 Python 3.12 运行/测试依赖约束，Mac 与 CI 复用。桌面打包附加依赖和构建隔离工具尚未完整锁定，不宣称哈希级供应链锁定。
- L04–L05、L08 后续：移除 README 过时图库数量，修正版本/预加载描述；安装包不再包含个人现场 README；冒烟测试使用代码 schema 常量；Ruff 扩展到 scripts。
- 验证：Python 187 项（1 项因本机 Windows 符号链接权限跳过），前端 102 项，Ruff src/tests/scripts、生产构建。所有新增写入测试均隔离，未启动正式服务、未升级正式库、未修改真实照片。

## 剩余规划与实施边界

| 项目 | 现状与下一步 |
|---|---|
| M04 归档安全放弃/撤销 | 现有恢复继续执行及损坏记录保护已覆盖；新增“放弃/撤销”涉及真实文件，先定义各阶段可恢复条件和人工确认，不能直接删除计划解锁。 |
| M05 突然断电 | 常规异常补偿已修复；跨数据库/JSON 的崩溃一致性仍需持久恢复协议及强制终止测试。 |
| M06 备份范围 | v1 范围不变，不含完整相册定义/归属/相册设备关联；下一步设计 v2 与兼容预检，保留旧格式读取。 |
| M09 长导出 | 暂停/取消响应已修复；普通写入仍可能等待导出，后续测量后决定独立导出任务与安全关闭的在途计数。 |
| M15–M16 工程结构 | 当前 schema 连接跳过 DDL，导入事务抽成独立模块；逐版本迁移、其他路由/前端/CSS 拆分仍按领域分批，不做无行为收益的整库改写。 |
| M17–M18 发布验证 | 已固定运行/测试依赖并补数据失败回归；Mac/干净 Windows 包验收、真实浏览器响应式/焦点/视觉回归尚未实测。 |
| M19 保留策略 | 尚不删除任何旧预览、报告、ZIP 或备份；先划分可重建与恢复必需数据、制定保留期限，再提供清理预览。 |
| 公共文档 | 安装包已去除现场 README；仓库维护文档仍保留本机历史路径和验收记录，公开分发前应单独整理，不抹去安全约束或伪称已清理 Git 历史。 |

以上是后续独立设计或实机验收事项，不等于已执行。下一次正式启动需在任务空闲时核对 schema 32→34 自动备份、健康接口和数据库完整性；不必重新归档或全量分析。
