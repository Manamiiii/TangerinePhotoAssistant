# 2026-09-22 质量修复验收清单

正式服务已经加载代码 `54c86ae`，schema 35，任务空闲。后续文档提交不改变运行代码。
请在方便时自行使用原有浏览器/窗口刷新页面；助手没有打开、移动或激活窗口。

## 必要检查（约 5 分钟，不改照片或人工数据）

- [ ] **应用设置 → 版本与更新**：数据库显示 `schema 35`，构建显示 `54c86ae` 开头，
  没有“含本地改动”；页面没有持续加载或报错。
- [ ] **照片图库**：翻下一页再返回，缩略图、分页位置与已验收布局正常；打开一张详情再关闭，
  当前筛选/分页仍符合预期。不需要重新扫描。
- [ ] **相册**：打开 `2026-08-22 · 她的生日`，可浏览照片和相似组；查看双图对比，
  左右/上下/单图切换可用，与“调整分组”的入口布局无异常。只查看，不重新归档或修改选片。
- [ ] **系统维护**：人工数据备份说明明确列出不包含相册定义、相册归属及相册器材关联，
  不会暗示这是完整工作目录备份；设备管理仍能看到原有配置。

## 可选检查（不影响本轮数据安全验收）

- [ ] 需要留一份新的人工备份时，点击“导出人工数据”、下载 JSON，再选择该文件执行预检。
  应显示匹配/缺失数量及是否替换设备配置；换文件后旧确认文字清空。
  **到预检即可，不需要点击“恢复人工数据”。**
- [ ] 日后实际需要照片导出时，观察 ZIP 生成期间评分/标签操作是否仍响应；归档、恢复和
  安全退出应显示等待导出完成。并发保护已在模拟慢导出的自动测试中验证，不需要为验收制造大量导出或改评分。

## 已由助手完成，无需用户重做

- 200 项 Python 回归（1 项 Windows 符号链接权限跳过）、108 项前端回归、Ruff、生产构建。
- 隐藏子进程在恢复写入前、写入后和数据库提交后退出；已有/没有设备配置两种情况均验证，
  包含重复恢复、外部改写保护、坏记录保护、会话中写入保护和安全停止。
- schema 35 升级前自动备份保留并校验；主备库完整性/外键、全业务表内容摘要及设备配置摘要核对。
- 正式图库分页无重叠、末页正确、相册 52 待整理索引为 0、无 pending；正式人工数据预检有效。
- 未改动真实照片、`D:\Photo`、XMP 或 Lightroom；未启动全量分析或 GPU 任务。

## 可选手工清理

早期失败测试的以下目录已核实只含临时库和模拟配置、没有照片。自动审批拒绝永久删除，
助手没有绕过，也没有把它们报告为已删除。若需要清理磁盘，可由用户自行删除：

- `C:\Users\WangYH\AppData\Local\Temp\tmp2bkzrq32`
- `C:\Users\WangYH\AppData\Local\Temp\tmpiz0jl2fs`

这不是功能验收的必要步骤，不要据此删除其他临时目录、正式备份或归档凭据。
此前移出项目的旧测试资料保留状态不变，详见 `FORMAL_ACCEPTANCE_20260922.md`。

## 本轮之后

当前进入用户界面验收阶段。请反馈失败项的操作、看到的现象及预期；无需重复运行自动测试。
备份 v2（完整相册覆盖）、归档安全撤销、自动保留策略、公开签名发布和其余大范围结构拆分
仍是独立后续事项，不是这次清理已经交付的能力，也不需要为本次验收启动它们。
