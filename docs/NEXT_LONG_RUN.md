# 下周末长任务准备

更新日期：2026-08-14。本文根据正式服务的只读接口生成，不代表已经授权启动任务。

## 当前实际基线

- 正式服务仍运行 schema 20 / `photo-critique-v4`，后台任务为空闲；本轮代码目标 schema 为 26。
- 13,809 个 Capture，其中 13,806 个有 JPG 并已完成技术质量分析，错误 0。
- 详情元数据待补 0，亮度直方图待补 0，不需要再次运行详情补全或全量质量分析。
- 已有 10,272 个视觉指纹，错误 0；2,595 个相似组包含 7,394 张照片。
- 大模型已有 585 条完成结果、覆盖 546 张照片；最近一批为 v4，50/50 成功，平均 41.14 秒/张。
- 当前人工入选、人工排除和人工星级样本均为 0，成长与多样性评估首先缺少的是人工基准，
  不是更多自动分数。

## 回归通过后再执行

1. 正常重启新版服务，让 schema 20 升级到 26；核对升级前备份、备份与活动库
   `PRAGMA integrity_check=ok`、健康接口和任务空闲。不得通过测试脚本直接打开正式数据库升级。
2. 完成 `WINDOWS_ACCEPTANCE.md` 第 48–50 项，并人工处理至少 10–20 个相似组；其中至少
   3 组完整结束，以验证选片会话统计。给部分入选照片记录保留理由。
3. 对至少 20–30 张照片给出人工星级，并复核 5–10 个参数化修图方案。数据量仍不足时只展示
   样本数，不据此修改排序权重或生成成长结论。

## 周末可长时间执行的任务

### A. 只读完整性复核（不使用 GPU）

- 先检查活动图库基线，再检查历史原片基线；两项顺序执行，避免同时占满磁盘。
- 只报告缺失、变化和新增，不自动修复、复制、移动或删除。
- 若发现差异，立即停在报告阶段等待人工确认。

### B. 现有模型结果整理（短任务，不使用 GPU）

- 同步现有可信 v4 结果的分析题材，再核对人工与导入标签没有被覆盖。
- 汇总提示词版本、结构警告和人工复核分布；不重跑旧照片。

### C. v5 推荐集分析（需要届时明确授权，使用 GPU）

- 启动前再次检查 `/api/tasks/current`、模型完整性、数据库备份空间和 GPU 竞争进程。
- 先跑 10 张快速验证，并人工检查题材多标签、拍摄建议和 8 项模型修图起点。
- 通过后按 100 → 200 → 500 分批，不直接运行全图库。按最近 41.14 秒/张估算，500 张约
  5.7 小时；按历史较慢的 52 秒/张估算约 7.2 小时。
- 每批结束检查成功率、重试数、结构警告和显存释放，再决定是否继续下一批。
- ComfyUI 或其他 GPU 任务出现时不启动新批次；运行中使用照片边界暂停，不强杀进程。

## 本次不安排

- 不运行新的全量 13,806 张模型分析；按当前速度需要约 158 小时，收益和人工复核能力不匹配。
- 不执行 Lightroom/XMP 写入、公共发布、人脸聚类、照片移动或删除。
- 不重跑已经完成且错误为 0 的全量技术质量、详情元数据和直方图任务。

## 长任务结束后的验收

- 后台任务回到 idle，数据库和备份完整性为 `ok`。
- 导出本批 CSV/JSON 报告，记录完成数、失败数、平均秒/张、重试和提示词版本。
- 抽查人工结论、题材标签、修图方案与统计是否仍按 Capture 同步，不写照片或 XMP。
- 形成下一次人工复核队列；不根据模型分数自动排除照片。
