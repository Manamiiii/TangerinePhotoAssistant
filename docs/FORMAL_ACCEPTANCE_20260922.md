# 2026-09-22 本机正式库收口

## 运行与清理结果

- 本机日常入口仅保留根目录 `config.toml` 与正式库。已移除演示照片、生成器、Mac 配置/启动脚本/冒烟工作流，以及持久桌面空库冒烟脚本。
- 核心自动化测试保留，使用临时目录并自动清理。本轮 Python 184 项（1 项 Windows 符号链接权限跳过）、前端 102 项、Ruff src/tests/scripts、生产构建通过；测试数量减少 3 是移除演示生成器专用测试。
- 永久删除未跟踪测试目录被自动审批拒绝，已可恢复地移出活动工程。两个待删除目录为项目同级 `TangerinePhotoAssistant-TestData-20260922`，以及 `%LOCALAPPDATA%/TangerinePhotoAssistant-TestData-20260922`；合计 943 个文件、293,906,821 字节，尚未释放磁盘空间。
- 正式归档凭据、正式性能记录、安装包、配置和备份均保留。其他项目的临时目录未处理。Git 历史未改写，历史提交仍可包含样片。

## 正式库升级与数据验证

- 通过应用自身的隐藏后端启动协议加载版本 `0e4fe7a`，没有 GUI、浏览器或鼠标操作。
- 正式主库 schema 32→34；自动备份 `catalog-pre-schema34-from32-20260922-081214-568086.sqlite3` 位于正式工作区 `Backups/AnalysisDatabase`。
- 主库与备份分别执行只读 `PRAGMA integrity_check` 和 `foreign_key_check`：均为 ok、0 问题。
- 升级前所有既有表（版本表除外）的行数及按主键排序后的完整行内容 SHA-256 与主库、备份均一致。
- 保留 14,287 个 Capture；新增 14,287 条当前路径别名；待恢复导入 0；照片索引指向旧演示目录的记录 0。
- 活动目录来自数据库的 `library_state`，为正式活动图库。没有扫描、移动、重命名或删除真实照片。

## 接口验收

- 健康接口 schema 34、local-only；当前任务 idle；启动审计补齐 complete、processed=0，没有启动模型。
- 可见图库 14,284 张；前两页各 40 张、ID 无交集；最后一页 offset=14280，共 4 张。
- 相册共 52 个；生日相册 52 的索引归档状态为 956 文件、0 个待整理路径、无 pending。此查询没有重新逐文件校验原图，不把它当作新的物理文件完整性证明。
- 后端保持运行于 127.0.0.1:8765，用户可在准备使用时自行打开。此次未做可见窗口/鼠标验收。

本地逐表摘要与接口核对结果保存在 `runtime/maintenance`，这是正式库维护记录，不属于演示数据。
